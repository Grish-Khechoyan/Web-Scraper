name: Build and scan Docker image

on:
  workflow_call:
    inputs:
      image:
        description: "Full Docker image name with tag"
        required: true
        type: string
    secrets:
      DOCKERHUB_USERNAME:
        required: true
      DOCKERHUB_TOKEN:
        required: true

jobs:
  build:
    runs-on: self-hosted
    outputs:
      tag: ${{ steps.extract-tag.outputs.tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - id: extract-tag
        run: |
          IMAGE="${{ inputs.image }}"
          echo "tag=${IMAGE##*:}" >> "$GITHUB_OUTPUT"

      # Replaced the step output with direct inline usage for cleaner code
      - name: Build image
        run: docker build -t "${{ secrets.DOCKERHUB_USERNAME }}/${{ inputs.image }}" .

      - name: Push image
        run: docker push "${{ secrets.DOCKERHUB_USERNAME }}/${{ inputs.image }}"

  scan:
    needs: build
    runs-on: self-hosted
    steps:
      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          # Directly referencing the secret and input instead of relying on the build job's output
          image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/${{ inputs.image }}
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '0'